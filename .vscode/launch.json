{
    "version": "0.2.0",
    "configurations": [
        
      {
        "name": "Run practice",
        "type": "cppdbg",
        "request": "launch",
        "program": "${fileDirname}/${fileBasenameNoExtension}",
        "args": [],            // command‑line args go here
        "stopAtEntry": false,
        "cwd": "${workspaceFolder}",
        "environment": [],
        "externalConsole": false,  // set true if you want a separate console
        "MIMode": "lldb",          // or "gdb" on Linux
        "preLaunchTask": "clang++ build active file"
      }
    ]
  }